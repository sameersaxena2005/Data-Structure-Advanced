# 34. Write a code that takes a tuple and an element as input and returns the count of occurrences of that element in the tuple
t = (1, 2, 3, 2, 4, 2, 5)
element = 2

count = t.count(element)
print(count)
