# 30. Create a code that takes a tuple and two integers as input. The function should return a new tuple containing elements 
# within the given range of indices
t = (1, 2, 3, 4, 5, 6)
start, end = 1, 4

new_tuple = t[start:end+1]
print(new_tuple)
