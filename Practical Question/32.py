# 32. Develop a code that takes a tuple of integers as input and returns the maximum and minimum values 
# using tuple unpacking
t = (3, 1, 4, 1, 5, 9)
min_val, max_val = min(t), max(t)

print(min_val, max_val)
